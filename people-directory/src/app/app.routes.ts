// app.routes.ts
import { Routes } from '@angular/router';
import { UserGridComponent } from './components/user-grid/user-grid'; // adjust path if needed

export const routes: Routes = [
  { path: '', component: UserGridComponent },
];